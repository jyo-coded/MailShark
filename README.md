# MailShark threat feeds

Generated daily by `.github/workflows/feeds.yml` on `main`; do not edit. Each list is a Bloom
filter (see `feeds/build_feeds.py`). Sources and licences are recorded in `v1/manifest.json`.
